runtime: php83

automatic_scaling:
  max_instances: 1

handlers:
- url: /(.+\.(css|js|png|jpg|jpeg|gif|ico|svg))
  static_files: \1
  upload: .+\.(css|js|png|jpg|jpeg|gif|ico|svg)

- url: /.*
  script: auto